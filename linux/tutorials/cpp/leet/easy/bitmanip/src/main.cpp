#include "leet1.h"
#include "print.h"

#include <stdint.h>

using namespace Print;

int main() {
    //vector<int> vec = countBits(5);
    //for(auto i : vec) {
    //    print(i);
    //}
    //int n = 0x0007;
    //print(n);
    //print(isPowerOfFour(n));
    print("0x", toHex(0));
    print("0x", toHex(0xfffffffe));
    return 0;
}
